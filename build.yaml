targets:
  $default:
    builders:
      # Drift: emit snake_case SQL identifiers so generated columns match the
      # DDL in design/01-data-and-persistence.md (e.g. dueDate -> due_date).
      drift_dev:
        options:
          case_from_dart_to_sql: snake_case
          named_parameters: true
